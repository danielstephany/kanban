export const base = `
    *, *::before, *::after {
        box-sizing: border-box;
    }

    html {
        font-size: 16px;
        font-family: "Roboto", sans-serif;
    }

    img {
        max-width: 100%;
    }
`